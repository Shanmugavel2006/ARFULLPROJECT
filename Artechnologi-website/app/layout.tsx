import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' })

export const metadata: Metadata = {
  title: 'AR Technology | Building Scalable Digital Products for the Future',
  description:
    'AR Technology is a product-based technology company delivering enterprise-grade digital solutions, cloud platforms, and AI-powered products to transform businesses worldwide.',
  keywords: ['enterprise technology', 'digital products', 'cloud solutions', 'AI platform', 'software engineering'],
  icons: {
    icon: [],
    apple: [],
  },
}

export const viewport: Viewport = {
  themeColor: '#FFFFFF',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased bg-background text-foreground">
        {children}
        <Analytics />
      </body>
    </html>
  )
}
